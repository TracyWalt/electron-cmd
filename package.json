{
  "name": "electron-cmd",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "cmd": "set NODE_ENV=dev&&electron .",
    "build": "electron-packager . CmdSprite --platform=win32 --arch=x64 --icon=icon.ico --out=./build --asar --app-version=0.0.1 --overwrite --ignore=node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^9.4.0",
    "electron-packager": "^14.0.4"
  }
}
